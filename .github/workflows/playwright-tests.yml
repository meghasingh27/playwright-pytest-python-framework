name: Playwright Tests

on:
  push:
    branches:
      - main
      - ci/github-actions

jobs:
  playwright-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4